# eightfold-transformer

Minimal scaffold for the candidate transformer pipeline.

Quick run:

```
python cli.py --inputs sample_inputs/recruiter.csv sample_inputs/ats.json sample_inputs/resume.txt --config config_examples/default_config.json
```
